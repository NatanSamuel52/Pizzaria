<Window x:Class="Pizzaria.Views.CadastroCliente"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cadastro de Clientes" Height="450" Width="650" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Formulário -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Nome:" FontWeight="Bold"/>
            <TextBox x:Name="txtNome" Margin="0,0,0,10"/>

            <TextBlock Text="Telefone:" FontWeight="Bold"/>
            <TextBox x:Name="txtTelefone" Margin="0,0,0,10"/>

            <TextBlock Text="Endereço:" FontWeight="Bold"/>
            <TextBox x:Name="txtEndereco" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
                <Button Content="Cadastrar" Width="100" Margin="0,0,10,0" Click="Cadastrar_Click"/>
                <Button Content="Editar" Width="100" Margin="0,0,10,0" Click="Editar_Click"/>
                <Button Content="Excluir" Width="100" Click="Excluir_Click"/>
            </StackPanel>
        </StackPanel>

        <!-- Lista -->
        <DataGrid x:Name="dgClientes" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="dgClientes_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="150"/>
                <DataGridTextColumn Header="Telefone" Binding="{Binding Telefone}" Width="100"/>
                <DataGridTextColumn Header="Endereço" Binding="{Binding Endereco}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
